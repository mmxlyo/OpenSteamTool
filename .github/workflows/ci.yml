name: CI
run-name: ${{ (github.ref_name == 'test' || github.base_ref == 'test') && format('[Test] CI - {0}', github.ref_name) || format('CI - {0}', github.ref_name) }}

on:
  pull_request:
    branches: [main, test]
  push:
    branches: [main, test]

# Cancel previous runs on the same PR / branch when new commits arrive.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ (github.ref_name == 'test' || github.base_ref == 'test') && '[Test] ' || '' }}Build (${{ matrix.config }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        config: [Release, Debug]

    steps:
      - uses: actions/checkout@v4

      - uses: ilammy/msvc-dev-cmd@v1

      - name: Configure
        run: cmake -S src -B build -G "Ninja Multi-Config"

      - name: Build ${{ matrix.config }}
        run: cmake --build build --config ${{ matrix.config }}

      - name: Upload artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ (github.ref_name == 'test' || github.base_ref == 'test') && '[Test] ' || '' }}OpenSteamTool-${{ matrix.config }}-pr${{ github.event.pull_request.number || github.run_number }}
          path: build/${{ matrix.config }}/
          retention-days: 7